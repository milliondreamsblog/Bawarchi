import { ComingSoon } from "@/components/ComingSoon";

export default function TablesScreen() {
  return (
    <ComingSoon
      title="Tables"
      hint="Create tables and download their QR codes. Use the web admin for now."
    />
  );
}
