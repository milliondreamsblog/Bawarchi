import { ComingSoon } from "@/components/ComingSoon";

export default function InventoryScreen() {
  return (
    <ComingSoon
      title="Inventory"
      hint="Stock levels and availability toggles per item. Use the web admin for now."
    />
  );
}
